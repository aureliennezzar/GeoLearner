const { default: Map } = require("domain/Map/Map");
const { default: MapContainer } = require("domain/MapContainer/MapContainer");

function App() {
  return (
    <div className="App">
      <MapContainer />
    </div>
  );
}

export default App;
